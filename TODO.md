# TODO

## Low Priority

- [x] Add TLS toggle to CLI (`--tls true|false`) and make `DTService` support insecure (plaintext) channels
- [x] Add `--compression true|false` to CLI and pass `grpc.Compression.Gzip` through to channels
